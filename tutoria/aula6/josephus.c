#include <stdio.h>

int main() {
    int n, k;
    printf("Entre com o número de pessoas (n) e o valor de passos (k): ");
    scanf("%d %d", &n, &k);

    int sobrevivente;

    printf("A posição do sobrevivente é: %d\n", sobrevivente);
    return 0;
}